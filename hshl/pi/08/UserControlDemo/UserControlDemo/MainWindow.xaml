<Window x:Class="UserControlDemo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UserControlDemo"
        mc:Ignorable="d"
        Title="MainWindow" Height="200" Width="500">
    <StackPanel Margin="10">
        <local:LabelAndTextbox Margin="5" Label="Vorname:" Text="{Binding Vorname}" />
        <local:LabelAndTextbox Margin="5" Label="Nachname:" Text="{Binding Nachname}"  />
        <local:LabelAndTextbox Margin="5" Label="E-Mail:" Text="{Binding EMail}" />
        <Button Margin="5" Click="Button_Click">Ändern</Button>
    </StackPanel>
</Window>
